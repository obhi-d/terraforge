
layout( local_size_x = BLOCK_SIZE, local_size_y = BLOCK_SIZE, local_size_z = 1 ) in;

// Output buffer
layout( std430, binding = Binding_Output ) buffer b_heights
{
    float4 values[];
}heights;

// Uniform buffer
layout( std430, binding = Binding_EnvUniforms ) uniform u_env
{
    env_params data;
}env;
